# Build outputs and rendered frames. The .java sources and the harness are the
*.class
# inputs; everything here is regenerated by running them.
japplet
java/test/*.jar
jclass_dump
jevents
jgfx_demo
jrun
jrun_asan
# JVM produced and is committed, so the comparison still means something on a
# libcheck.sh writes what this runtime produced here; ref/ holds what a real
# machine with no JDK installed.
*.o
out/
*.png
*.ppm
